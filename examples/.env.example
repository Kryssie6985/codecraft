# Example .env for Hugging Face demo
# Copy this to a file named .env and replace with your real token.
HF_API_TOKEN=hf_xxx_your_token_here

# IMPORTANT: Do NOT commit the real .env file. Add .env to .gitignore if it's not already ignored.
